package com.formacionbdi.microservicios.app.cursos.models.enity;


public class NotasPersonales {

/*

Por ejemplo, si queremos comprobar que un objeto "persona" no es una instancia de la clase "Estudiante", 
podemos utilizar el siguiente código:

if (!(persona instanceof Estudiante)) {
    System.out.println("El objeto no es una instancia de la clase Estudiante");
}

==========================================================================================

List<String> lista = Arrays.asList("Hola", "Mundo", "!");

lista.forEach(elemento -> {
    System.out.println(elemento);
});


===========================================================================================

Errores personales que afectan el funcionamiento con Spring

* En los metodos Setter evitar modificar a nombres personalizados, dado que puede originar
  errores
  
  private String elTexto; ERROR
  private String texto;
  
  
* Un error en la nomenclatura —private Asignatura asignatura; // asignaturaExm, no —

 */
}
